const config = {
  consumer_key:         '',        //API keys, need to get your own if you want this to run
  consumer_secret:      '',
  access_token:         '',       //Access token keys, need to get your own if you want this to run
  access_token_secret:  ''
}

module.exports = config;